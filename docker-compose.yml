version: '3.8'
services:
  app:
    build: .
    container_name: resume-app
    restart: always

  nginx:
    image: nginx:latest 
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
